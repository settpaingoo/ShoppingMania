<ul>
<% @items.each do |item| %>
  <li>
    <%= link_to item.name, item_url(item) %>
    Price: $<%= item.price %>
  </li>
<% end %>
</ul>

<%= render partial: "filter_items_form",
  locals: { items: @items, brands: Brand.all, categories: Category.all }%>